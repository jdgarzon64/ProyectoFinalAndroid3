<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="Fragments.RegistrarHectarea"
    android:layout_marginTop="120dp">

    <!-- TODO: Update blank fragment layout -->

    <EditText
        android:id="@+id/nombreHectarea"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ems="10"
        android:hint="Nombre"
        android:inputType="textPersonName"
        />

    <ImageView
        android:id="@+id/fotoHectarea"
        android:layout_width="171dp"
        android:layout_height="128dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="60dp"
        android:src="@drawable/ic_menu_camera" />

    <Button
        android:id="@+id/agregarUbicacion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/registrarHectarea"
        android:layout_alignParentStart="true"
        android:text="Agregar Ubicacion" />

    <Button
        android:id="@+id/tomarFotoHectarea"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/agregarUbicacion"
        android:layout_alignParentStart="true"
        android:text="Tomar Foto" />

    <Button
        android:id="@+id/registrarHectarea"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="23dp"
        android:text="Registrar Hectarea" />
</RelativeLayout>
